% Generated by roxygen2: do not edit by hand
\name{show_tissues}
\alias{show_tissues}
\title{Show the tissues available in cellNexus}
\usage{
show_tissues(
  metadata = NULL,
  cloud_metadata = get_metadata_url("hca_2024"),
  local_metadata = NULL,
  cache_directory = get_default_cache_dir(),
  use_cache = TRUE
)
}
\arguments{
\item{metadata}{Optional metadata table returned by \code{\link[=get_metadata]{get_metadata()}}. If
\code{NULL}, metadata is retrieved using the remaining arguments.}

\item{cloud_metadata,local_metadata,cache_directory,use_cache}{Arguments
passed to \code{\link[=get_metadata]{get_metadata()}} when \code{metadata} is \code{NULL}.}
}
\value{
A tibble with \code{tissue} and \code{tissue_groups} columns.
}
\description{
Returns the tissue and tissue group combinations available in the
harmonised metadata database.
}
\examples{
show_tissues()
}
