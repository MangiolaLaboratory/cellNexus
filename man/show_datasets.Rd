% Generated by roxygen2: do not edit by hand
\name{show_datasets}
\alias{show_datasets}
\title{Show datasets available for a tissue}
\usage{
show_datasets(
  tissue = NULL,
  tissue_groups = NULL,
  metadata = NULL,
  cloud_metadata = get_metadata_url("hca_2024"),
  local_metadata = NULL,
  cache_directory = get_default_cache_dir(),
  use_cache = TRUE
)
}
\arguments{
\item{tissue}{Optional character vector of tissues to include.}

\item{tissue_groups}{Optional character vector of tissue groups to include.}

\item{metadata}{Optional metadata table returned by \code{\link[=get_metadata]{get_metadata()}}. If
\code{NULL}, metadata is retrieved using the remaining arguments.}

\item{cloud_metadata,local_metadata,cache_directory,use_cache}{Arguments
passed to \code{\link[=get_metadata]{get_metadata()}} when \code{metadata} is \code{NULL}.}
}
\value{
A tibble with \code{dataset_id}, \code{tissue}, and \code{tissue_groups} columns.
}
\description{
Returns the datasets available in the harmonised metadata database,
optionally restricted to one or more tissues and tissue groups.
}
\examples{
show_datasets(tissue_groups = "blood")
}
